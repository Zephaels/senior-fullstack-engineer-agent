# Behavior evaluation results

This directory intentionally contains no fabricated model scores. Use the included runner with an authenticated model or CLI, then store dated, model-specific results here. Mock transport tests validate harness plumbing only.
