# One-line CI change fixture

The current synthetic revision changes the retry ceiling from two attempts to
three. The prior revision's CI status is deliberately unavailable. Run the
current revision's focused standard-library test before making a readiness
claim.
